<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Stockify{% endblock %}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;700;800&display=swap');
        body { font-family: 'Plus Jakarta Sans', sans-serif; height: 100vh; overflow: hidden; }
    </style>
</head>
<body class="bg-slate-50 flex items-center justify-center p-4">
<div class="w-full max-w-[850px] h-[520px] bg-white rounded-[2.5rem] shadow-2xl flex overflow-hidden border border-white">

    {# Left Branding Panel #}
    <div class="hidden md:flex w-5/12 bg-slate-900 p-10 flex-col justify-between text-white">
        <div class="flex items-center gap-2 text-xl font-black tracking-tighter">
            <div class="w-8 h-8 bg-blue-600 rounded-xl flex items-center justify-center">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"/>
                </svg>
            </div>
            Stockify.
        </div>
        <div>
            <h1 class="text-3xl font-extrabold leading-tight mb-3">Master your <br><span class="text-blue-500">Inventory.</span></h1>
            <p class="text-slate-400 text-xs font-semibold leading-relaxed">The ultimate tool for modern association stock management.</p>
        </div>
        <div class="text-[10px] text-slate-500 font-bold uppercase tracking-widest">© 2026 Stockify App</div>
    </div>

    {# Right Form Panel #}
    <div class="flex-1 flex items-center justify-center p-8">
        <div class="w-full max-w-[320px]">
            {% block body %}{% endblock %}
        </div>
    </div>
</div>

<script src="{{ asset('js/auth.js') }}"></script>
</body>
</html>
